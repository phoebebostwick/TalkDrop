using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class README : MonoBehaviour {

//	This is the most up to date project folder
//	There is a readme file in every scene that explains how to run / test each scene + what exactly is going on

//	When Creating ReadMe's put use this format -> SceneName_ReadMe
//	If this is a duplicated scene do M_SceneName_ReadMe (and replace first letter w ur first initial; Luu and Russo should use L and R)
	//  When duplicating the necessary scripts follow the same format (ex: mScriptName_SceneName)
	// For asset naming use ObjectDescription_SceneName (ThotBubbleMaterial_OnBoardingScene)







	
	// Use this for initialization
	void Start () {

	}

	// Update is called once per frame
	void Update () {

	}
}
